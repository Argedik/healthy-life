https://www.rust-lang.org/tr/tools/install
yukarıdaki siteden önce `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh` kodunu yazarak rust'u yükleyin.

rustc --version
cargo --version

cargo install trunk
